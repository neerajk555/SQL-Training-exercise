# Module 14 · Triggers

Master database triggers: BEFORE/AFTER INSERT/UPDATE/DELETE, audit logging, data validation, and automation.

## What's inside
8 activity files covering: CREATE TRIGGER, BEFORE/AFTER triggers, NEW/OLD references, Audit logging, Validation, Cascade operations

## Topics: Trigger types, Timing (BEFORE/AFTER), Events (INSERT/UPDATE/DELETE), NEW and OLD values, Use cases, Best practices

**Encouragement:** Triggers automate database logic—use wisely for consistency! 🚀
